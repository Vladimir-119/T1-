# 🚀 VibeCode — AI Interview Simulator

**VibeCode** — это интерактивная платформа для симуляции технических собеседований с искусственным интеллектом. Система генерирует уникальные алгоритмические задачи, проверяет код, оценивает сложность алгоритмов (Big O), проводит Code Review и анализирует Soft Skills кандидата.

---

## 📂 Структура проекта

Проект реализован на микросервисной архитектуре и упакован в Docker:

- **`backend.py`** — Основной API Gateway (FastAPI).
- **`vibecode-backend/`** — Сервис с логикой интервью, интеграцией LLM и проверкой кода.
- **`vibecode-frontend/`** — Клиентское приложение (React + TypeScript + Vite).
- **`docker-compose.yml`** — Конфигурация для оркестрации всех контейнеров.

---

## 🛠 Технологический стек

- **Backend**: Python 3.11, FastAPI, OpenAI/Compatible API, NumPy.
- **Frontend**: React 18, TypeScript, Monaco Editor, TailwindCSS.
- **DevOps**: Docker, Docker Compose.

---

## 🚀 Инструкция по запуску

### 1. Предварительные требования
Убедитесь, что у вас установлены:
- **Docker** и **Docker Compose**
- **Git**

### 2. Установка и запуск
Склонируйте репозиторий и запустите проект одной командой:

``git clone https://github.com/Vladimir-119/T1-.git
cd T1-/T1``


2. **Запустите проект одной командой:**
``docker-compose up --build``


3. **Откройте приложение:**
👉 [**http://localhost:5173**](http://localhost:5173)

---

## 🛠 Способ 2: Ручной запуск (без Docker)

Если вы хотите запустить проект локально для разработки.

### 1. Backend (Python)

Вам понадобятся следующие библиотеки. Вы можете установить их вручную или через `requirements.txt`.

**Необходимые библиотеки (`requirements.txt`):**
fastapi
uvicorn
pydantic
openai
numpy
python-multipart
requests
python-dotenv

``pip install -r requirements.txt``
